{
  "name": "pdf-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon src/index.js --watch ./src/**/*.*",
    "start:preview": "cross-env NODE_ENV=development MODE=preview node src/index.js",
    "start:preview:watch": "cross-env NODE_ENV=development MODE=preview nodemon src/index.js --watch ./src/**/*.*",
    "build": "cross-env NODE_ENV=production node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cross-env": "^7.0.3",
    "handlebars": "^4.7.8",
    "nodemon": "^3.0.1",
    "puppeteer": "^13.0.0"
  }
}
